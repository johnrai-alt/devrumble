"""
TODO: models for routing.
Coordinate table/column names with the DB team before writing these
(equivalent note to the one in routing.repository.js).
"""
from django.db import models  # noqa: F401

# TODO: define routing models here, e.g.:
# class RoutingThing(models.Model):
#     class Meta:
#         db_table = "routing"
